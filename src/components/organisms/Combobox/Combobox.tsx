import { Wrapper } from './Combobox.styles';

const Combobox = () => {
  return (
    <Wrapper>
      <input placeholder="Genre name" />
      <button>↓</button>
    </Wrapper>
  );
};

export default Combobox;
